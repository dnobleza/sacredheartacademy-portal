# Copy to .env in this folder. docker compose reads it automatically.
# Never commit .env.

# MySQL container
DB_ROOT_PASSWORD=
DB_NAME=sacred_heart_academy
DB_USER=sacred_heart
DB_PASSWORD=

# API container
CORS_ORIGINS=http://localhost:5173

# Generate each with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
JWT_REFRESH_SECRET=
